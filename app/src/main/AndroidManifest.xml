<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Aplicação Opinião PT01"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicacaoOpiniaoPT01"
        tools:targetApi="31">

        <!-- Detalhes de atividade existente -->
        <activity android:name=".ui.DetailActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- Definir LoginActivity como atividade principal -->
        <activity
            android:name="com.example.aplicaoopiniopt01.ui.LoginActivity"
            android:exported="true"
            android:label="Login"
            android:theme="@style/Theme.AplicacaoOpiniaoPT01"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity será lançada após login bem-sucedido -->
        <activity
            android:name="com.example.aplicaoopiniopt01.ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AplicacaoOpiniaoPT01"
            android:windowSoftInputMode="adjustResize">
        </activity>

    </application>

</manifest>
